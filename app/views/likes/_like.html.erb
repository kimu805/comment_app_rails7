<% if user_signed_in? && item.liked_by?(current_user) %>
  <%= link_to "いいね済み", item_likes_path(item.id), data: {turbo_method: :delete}, remote: true %>
<% else %>
  <%= link_to "いいねする", item_likes_path(item.id), data: {turbo_method: :post}, remote: true %>
<% end %>